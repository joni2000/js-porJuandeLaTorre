//objeto MATH
let resultado;

resultado = Math.PI;
resultado = Math.round(2.2); //sirve para redondear
resultado = Math.ceil(2.2); //sirve para redondear hacia arriba
resultado = Math.floor(2.9); //sirve para redondear hacia abajo
resultado = Math.sqrt(9) //raiz cuadrada
resultado = Math.abs(-200); //convierte al numero en posistivo
resultado = Math.min(9, 3, 5, 1, 8, 2, 6); //numero minimo del listado
resultado = Math.max(9, 3, 5, 1, 8, 2, 6); //numero maximo del listado
resultado = Math.random() //numero ramdon
resultado = Math.floor( Math.random()*30 );

console.log(resultado);